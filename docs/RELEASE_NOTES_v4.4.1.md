
# TradeComposer v4.4.1 — Full, Unified, Deploy‑Ready
- **Unified Partner API** in one `main.py` (coins/tokens/memecoins, derivatives, simulator, payments+entitlements).
- **Role‑gated navigation** in the web app (editor/admin only pages).
- **Deploy configs**: Dockerfile (API) + `cloudbuild.yaml` + Hosting.
- **Env examples** for quick bring‑up.
