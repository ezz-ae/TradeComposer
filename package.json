
{
  "private": true,
  "workspaces": ["apps/*","packages/*"],
  "scripts": {
    "build": "npm --prefix apps/web run build",
    "dev": "npm --prefix apps/web run dev",
    "dev:web": "npm --prefix apps/web run dev"
  },
  "devDependencies": { "@swc/core": "^1.3.100" }
}
